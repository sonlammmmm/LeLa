package com.lela.leaderboardsnapshot.domain;

// Enum kỳ xếp hạng leaderboard.
public enum LeaderboardPeriodType {
    DAILY, // Xếp hạng theo ngày.
    WEEKLY, // Xếp hạng theo tuần.
    MONTHLY, // Xếp hạng theo tháng.
    ALL_TIME // Xếp hạng toàn thời gian.
}
